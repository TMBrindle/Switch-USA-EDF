# Transmission Modeling Changes

This document records all changes made to add asymmetric transmission, new-build
capacity derating, hurdle costs, and planned project build minimums to the
ReEDS/Switch/PowerGenome pipeline.

---

## 1. New Switch Module — Hurdle Costs

**File (new):** `switch/study_modules/trans_hurdle_cost.py`

Adds a per-MWh operational cost to all power flows crossing hurdle-region boundaries.
Cost rates are read from `trans_hurdle_cost.csv` (generated by `pg_to_switch.py`) and
added to the objective function via `Cost_Components_Per_TP`. The cost applies
symmetrically to flows in both directions on each cross-boundary line.

**Input file produced:** `switch/in/<year>/<case>/trans_hurdle_cost.csv`
- Columns: `TRANSMISSION_LINE, PERIOD, trans_hurdle_cost_per_mwh`
- Sourced from `cost_hurdle_intra.csv` (hurdlereg column), matched to the closest
  available year for each model period.

---

## 2. New Switch Module — New-Build Capacity Derating

**File (new):** `switch/study_modules/trans_new_build_derate.py`

Applies a 15% derating factor (0.85) to new transmission capacity built across
planning subregion (`transgrp`) boundaries. Replaces the core
`Maximum_DispatchTx` constraint with a new `Maximum_DispatchTx_Derated` constraint
that uses:

```
DispatchTx ≤ existing_cap × derating_factor + new_cap × new_build_derate_factor
```

Within-subregion new builds use the default factor of 1.0 (no derating).

**Input file produced:** `switch/in/<year>/<case>/trans_new_build_derate.csv`
- Columns: `TRANSMISSION_LINE, trans_new_build_derate_factor`
- Only lines crossing a `transgrp` boundary are written (factor = 0.85).

**Note:** This module deactivates `Maximum_DispatchTx` from the core dispatch module
and replaces it. It must load after `switch_model.transmission.transport.dispatch`.

---

## 3. New Switch Module — Asymmetric Directional Capacity

**File (new):** `switch/study_modules/trans_asymmetric_capacity.py`

Adds supplementary per-direction capacity constraints on top of the symmetric
`Maximum_DispatchTx` constraint. The tighter of the two limits applies; for lines
not in the input file, this module has no effect.

The constraints represent existing infrastructure limits from the NARIS2024 dataset.
New transmission built via `BuildTx` increases the symmetric nameplate capacity
tracked by the core model; the directional constraint applies to total dispatch, so
new builds may relax asymmetric limits if built on an asymmetric corridor.

**Input file produced:** `switch/in/<year>/<case>/trans_directional_limits.csv`
- Columns: `zone_from, zone_to, PERIOD, trans_directional_cap_mw`
- One row per direction per period for each line that has an entry in the NARIS2024
  source files.

---

## 4. New Switch Module — Planned Build Minimums

**File (new):** `switch/study_modules/trans_build_minimum.py`

Enforces minimum new transmission capacity builds by a specified period, reflecting
planned or committed transmission projects. For each `(TRANSMISSION_LINE, PERIOD)`
entry in `trans_build_minimum.csv`, the cumulative new capacity built up to and
including that period must be at least `trans_build_minimum_mw` MW. Existing
capacity does not count — the constraint applies to new builds only.

The input file is optional; if absent the module has no effect.

**Input file (optional):** `switch/in/<year>/<case>/trans_build_minimum.csv`
- Columns: `TRANSMISSION_LINE, PERIOD, trans_build_minimum_mw`
- Generated by `pg_to_switch.py` from `transmission_connections.csv` when
  `build_minimum_policy = yes` (see §9). Lines with no target year are exempt from
  the minimum but still exempt from the zero-expansion limit (see §6e).

---

## 5. `modules.txt` — New Module Registrations

**File:** `switch/modules.txt`

Four new modules added:

After `switch_model.transmission.transport.dispatch`:
```
study_modules.trans_new_build_derate
study_modules.trans_hurdle_cost
study_modules.trans_asymmetric_capacity
```

After `study_modules.trans_path_expansion_limit`:
```
study_modules.trans_build_minimum
```

Load order is important: `trans_new_build_derate` must follow the core dispatch
module because it deactivates `Maximum_DispatchTx` via `BuildAction`.

---

## 6. `pg_to_switch.py` — Data-Generation Blocks

**File:** `pg_to_switch.py`

Five blocks were added inside the per-scenario loop to generate new input CSV
files. Four are gated by scenario-level policy flags (see §9).

### 6a. Planned project injection and cross-transreg blocking (inside hierarchy loop)
Reads `transmission_connections.csv` to identify planned/committed projects. For
each planned project, injects it into the transmission lines table if not already
present. Cross-transreg new builds are blocked (i.e. `trans_new_build_allowed = 0`)
for all non-planned lines when `transmission_policy = constrained`. Planned lines
with a target year generate `trans_build_minimum.csv` rows when
`build_minimum_policy = yes` (see §4).

### 6b. Derate block (inside hierarchy loop, gated by `degrade_policy`)
Iterates all transmission lines; for any line where `transgrp[lz1] != transgrp[lz2]`,
appends a row with `trans_new_build_derate_factor = 0.85` to `trans_new_build_derate.csv`.

### 6c. Hurdle-cost block (inside hierarchy loop, gated by `hurdle_policy`)
Reads `cost_hurdle_intra.csv`, selects the closest available year to each model
period, and appends rows for all cross-`hurdlereg` lines to `trans_hurdle_cost.csv`.

### 6d. Asymmetric-capacity block (outside hierarchy loop, gated by `asymmetry_policy`)
Reads `transmission_capacity_init_AC_ba_NARIS2024.csv` (directional `MW_f0`/`MW_r0`)
and `transmission_capacity_init_nonAC_ba.csv` (symmetric DC/B2B MW). Builds a lookup
by zone-pair and appends two rows (one per direction) per line per period to
`trans_directional_limits.csv`.

### 6e. Expansion limit block (three-branch policy)
Generates `trans_path_expansion_limit.csv` based on `trans_expansion_policy`:
- `zero` (default): all non-planned lines get 0 MW expansion allowance per period
- `nerc_growth`: per-period limit = cumulative capacity × average NERC regional
  growth % from `nerc_growth_pct.csv`; limits compound across periods
- `unlimited`: file is not written; Switch is unrestricted on expansion

Planned project lines (from `transmission_connections.csv`) are always exempt from
the expansion limit regardless of policy, so their build minimums are not blocked.

**Supporting lookups added to hierarchy block:**
```python
zone_transgrp  = dict(zip(hierarchy["ba"], hierarchy["transgrp"]))
zone_hurdlereg = dict(zip(hierarchy["ba"], hierarchy["hurdlereg"]))
```

---

## 7. REFS2009 → NARIS Migration

### 7a. `pg/settings/extra_inputs.yml`
Changed the transmission constraints source from REFS2009 to NARIS:

| Setting | Old value | New value |
|---|---|---|
| `user_transmission_costs` | `network_costs_ReEDS_patched_for_REFS2009.csv` | `network_costs_ReEDS.csv` |
| `user_transmission_constraints_fn` | `reeds_ba_tx_REFS2009_avg.csv` | `reeds_ba_tx_NARIS_avg.csv` |

### 7b. `patch_pg_resource_groups.py`
The last section (lines 291–323) was replaced to regenerate
`pg/extra_inputs/reeds_ba_tx_NARIS_avg.csv` from local source files instead of
downloading REFS2009 from GitHub:

- AC lines: averages `(MW_f0 + MW_r0) / 2` from
  `transmission_capacity_init_AC_ba_NARIS2024.csv` (292 corridors)
- DC/B2B lines: taken directly from `transmission_capacity_init_nonAC_ba.csv`
  (10 lines)
- Output: 302 rows with columns `region_from, region_to, firm_ttc_mw, notes`

**Known gap:** 8 corridors present in REFS2009 are absent from the NARIS2024 dataset:
`p10-p12, p13-p25, p15-p21, p15-p25, p16-p18, p41-p70, p55-p85, p128-p132`.
These are silently excluded from newly generated cases. None of the affected zones
are fully isolated (each has other connections), but the capacity on these corridors
is lost. Consider adding them back to `reeds_ba_tx_NARIS_avg.csv` manually if needed.

---

## 8. Scenario Toggle Columns

### 8a. `pg/extra_inputs/scenario_inputs.csv`
Six new columns appended, all defaulting to `constrained/zero/yes/yes/yes/yes` for
all existing rows:

| Column | Values | Controls |
|---|---|---|
| `transmission` | `constrained` / `unconstrained` | Whether cross-transreg new builds are blocked |
| `trans_expansion` | `zero` / `nerc_growth` / `unlimited` | Expansion limit policy for `trans_path_expansion_limit.csv` |
| `hurdle` | `yes` / `no` | Whether hurdle-cost data is written |
| `degrade` | `yes` / `no` | Whether new-build derate data is written |
| `asymmetry` | `yes` / `no` / `old` | `yes` = NARIS2024 with asymmetric directional limits; `no` = NARIS2024 symmetric; `old` = REFS2009 symmetric |
| `build_minimum` | `yes` / `no` | Whether planned project build minimums are enforced |

### 8b. `pg/settings/scenario_management.yml`
Six new mapping blocks added:

```yaml
transmission:
  constrained:
    transmission_policy: constrained
  unconstrained:
    transmission_policy: unconstrained
trans_expansion:
  zero:
    trans_expansion_policy: zero
  nerc_growth:
    trans_expansion_policy: nerc_growth
  unlimited:
    trans_expansion_policy: unlimited
hurdle:
  yes:
    hurdle_policy: yes
  no:
    hurdle_policy: no
degrade:
  yes:
    degrade_policy: yes
  no:
    degrade_policy: no
asymmetry:
  yes:
    asymmetry_policy: yes
  no:
    asymmetry_policy: no
  old:
    asymmetry_policy: no
    user_transmission_constraints_fn: reeds_ba_tx_REFS2009_avg.csv
    user_transmission_costs: network_costs_ReEDS_patched_for_REFS2009.csv
build_minimum:
  yes:
    build_minimum_policy: yes
  no:
    build_minimum_policy: no
```

The `asymmetry: old` option reverts both the transmission constraint file and the
network cost file to the REFS2009 dataset, which remains in `pg/extra_inputs/`.
No `trans_directional_limits.csv` is written in this case.

### 8c. Policy gates in `pg_to_switch.py`
Each feature block is conditioned on its policy setting (default shown):

```python
if settings.get("transmission_policy",  "constrained") == "constrained": ...  # cross-transreg blocking
if settings.get("build_minimum_policy", "yes") == "yes": ...
if settings.get("degrade_policy",       "yes") == "yes": ...
if settings.get("hurdle_policy",        "yes") == "yes": ...
if settings.get("asymmetry_policy",     "yes") == "yes": ...
# trans_expansion_policy drives the three-branch expansion limit logic (see §6e)
```

When `asymmetry_policy` is `no`, both NARIS source file paths are set to `None` so
the block is skipped entirely without touching the filesystem.

---

## 9. Source Data Files Added

| File | Description |
|---|---|
| `transmission_capacity_init_AC_ba_NARIS2024.csv` | 292 AC corridors with directional capacity (`MW_f0` = forward, `MW_r0` = reverse) from NARIS2024 |
| `transmission_capacity_init_nonAC_ba.csv` | 10 DC and B2B lines with a single symmetric MW value |
| `cost_hurdle_intra.csv` | $/MWh hurdle rates by hierarchy level and year (2010–2050); `hurdlereg` column used |
| `hierarchy.csv` | Zone → NERC/transreg/transgrp/hurdlereg mapping used by `pg_to_switch.py` |
| `transmission_connections.csv` | Planned/committed transmission projects with target year and MW |
| `nerc_growth_pct.csv` | NERC regional transmission growth rates by period, used for `nerc_growth` expansion policy |

---

# In-Zone Generation Ratio Constraints

---

## 10. New Switch Module — In-Zone Generation Ratio Constraints

**File (new):** `switch/study_modules/gen_zone_ratio.py`

Constrains in-zone annual generation as an optional fraction of in-zone annual load,
and/or in-zone generation as a fraction of contemporaneous load at every timepoint
(hourly peak share). Operates at two levels:

- **Zone-level** — applied per individual model zone (`LOAD_ZONE`)
- **Group-level** — applied to summed generation and load across a named set of model
  zones (e.g., all BAs in a state, utility service territory, or transmission region)

All constraints are optional per zone/period; omit a row or leave cells blank to leave
a constraint inactive. The module has no effect if none of the three input files are
present.

Pyomo constraints defined:

| Constraint | Level | Direction |
|---|---|---|
| `Zone_Min_Annual_Gen_Ratio` | zone | annual gen ≥ ratio × annual load |
| `Zone_Max_Annual_Gen_Ratio` | zone | annual gen ≤ ratio × annual load |
| `Zone_Min_Peak_Share` | zone | hourly gen ≥ share × hourly load (every timepoint) |
| `Zone_Max_Peak_Share` | zone | hourly gen ≤ share × hourly load (every timepoint) |
| `Group_Min_Annual_Gen_Ratio` | group | group annual gen ≥ ratio × group annual load |
| `Group_Max_Annual_Gen_Ratio` | group | group annual gen ≤ ratio × group annual load |
| `Group_Min_Peak_Share` | group | group hourly gen ≥ share × group hourly load |
| `Group_Max_Peak_Share` | group | group hourly gen ≤ share × group hourly load |

**Input files (all optional):** `switch/in/<year>/<case>/`

| File | Columns | Purpose |
|---|---|---|
| `gen_zone_load_ratio.csv` | `LOAD_ZONE, PERIOD, min_annual_ratio, max_annual_ratio, min_peak_share, max_peak_share` | Zone-level constraint bounds; extra `historical_*` reference columns are ignored |
| `gen_zone_groups.csv` | `GROUP_NAME, LOAD_ZONE` | Maps group names to their constituent model zones |
| `gen_group_load_ratio.csv` | `GROUP_NAME, PERIOD, min_annual_ratio, max_annual_ratio, min_peak_share, max_peak_share` | Group-level constraint bounds |

All three files are loaded via `apply_input_aliases()`, so `--input-alias` flags work
correctly (e.g., `--input-alias gen_group_load_ratio.csv=gen_group_load_ratio_max1.30.csv`).

**Output file:** `outputs/gen_zone_ratio_summary.csv`

Written after each solve. One row per zone/group per period containing actual solved
values alongside the applied constraint bounds.
- Columns: `TYPE, LOAD_ZONE, PERIOD, actual_annual_gen_twh, actual_annual_load_twh,
  actual_annual_ratio, min_annual_ratio, max_annual_ratio, actual_min_hourly_gen_share,
  actual_max_hourly_gen_share, min_peak_share, max_peak_share`
- `TYPE` is `zone` for individual zone rows and `group` for aggregated group rows.

---

## 11. `modules.txt` — gen_zone_ratio Registration

**File:** `switch/modules.txt`

One new module added, after `study_modules.trans_build_minimum`:

```
study_modules.gen_zone_ratio
```

No load-order dependency with other study modules; it reads only `DispatchGen`,
`zone_demand_mw`, and standard timescale sets from the core model.

---

## 12. Helper Script — `make_zone_ratios.py`

**File (new):** `make_zone_ratios.py`

Command-line utility that generates constraint input files from historical data.
Pulls EIA-923 generation by ReEDS BA from PUDL and load curves from PowerGenome's
`load_curves_nrel_reeds` table, computes 2020–2023 average annual and peak statistics,
applies a coverage adjustment factor (default 1.30×, correcting for ~19% EIA-923
undercounting and ~6% T&D losses), and writes scenario-ready CSV files.

Key flags:

| Flag | Purpose |
|---|---|
| `--agg-by <COLUMN>` | Aggregate BAs to a higher level (`state`, `transreg`, `nercr`, `transgrp`); generates both `gen_zone_groups.csv` and `gen_group_load_ratio.csv` |
| `--min-annual-ratio SPEC` | Formula to auto-populate `min_annual_ratio` (e.g., `"min*0.95"` = 95% of 4-year minimum) |
| `--max-annual-ratio SPEC` | Formula to auto-populate `max_annual_ratio` (e.g., `"max*1.30"`) |
| `--coverage-adjustment FACTOR` | Override the default 1.30× upward correction |
| `--output-dir PATH` | Write output files directly into a scenario inputs directory |

**Reference data files committed to the repo root** (pre-computed for two aggregation levels):

| File | Description |
|---|---|
| `gen_zone_load_ratio.csv` | BA-level reference ratios for all 134 ReEDS zones |
| `gen_zone_load_ratio_BA.csv` | Alternative BA-level variant |
| `gen_zone_load_ratio_hurdlereg.csv` | Aggregated to hurdling regions |
| `gen_zone_load_ratio_hurdlereg_check.csv` | Diagnostic check file for hurdlereg aggregation |
| `gen_zone_groups_hurdlereg.csv` | Zone → hurdlereg group membership mapping |

Full usage examples and worked scenario designs are in `gen_zone_ratio_instructions.txt`.
